# Python dependencies installed by ComfyUI-Manager: pip install -r requirements.txt
# Keep versions loose to avoid conflicts with other custom nodes.
# https://docs.comfy.org/custom-nodes/backend/manager

safetensors
huggingface-hub
onnxruntime-gpu

# Optional (uncomment per use):
# onnx
# onnxslim
# tensorrt
#   TensorRT engines are tied to the exact TensorRT version (and GPU) that
#   built them. Use the same version to build and run engines; after upgrading
#   TensorRT, rebuild your engines.
